<Window x:Class="CcDirector.Wpf.CloneRepoDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Clone Repository"
        Width="500" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#252526">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Repository URL:"
                   Foreground="#CCCCCC"
                   FontSize="14"
                   Margin="0,0,0,6" />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="UrlInput" Grid.Column="0"
                     Background="#1E1E1E"
                     Foreground="#CCCCCC"
                     CaretBrush="#CCCCCC"
                     BorderBrush="#3C3C3C"
                     Padding="8,6"
                     FontSize="13" />
            <Button Grid.Column="1" Content="Browse GitHub"
                    Click="BtnBrowseGitHub_Click"
                    Width="110" Margin="6,0,0,0"
                    Background="#3C3C3C"
                    Foreground="#CCCCCC"
                    BorderThickness="0"
                    Cursor="Hand" />
        </Grid>

        <TextBlock Grid.Row="2"
                   Text="Destination folder:"
                   Foreground="#CCCCCC"
                   FontSize="14"
                   Margin="0,14,0,6" />

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="DestInput" Grid.Column="0"
                     Background="#1E1E1E"
                     Foreground="#CCCCCC"
                     CaretBrush="#CCCCCC"
                     BorderBrush="#3C3C3C"
                     Padding="8,6"
                     FontSize="13" />
            <Button Grid.Column="1" Content="Browse..."
                    Click="BtnBrowse_Click"
                    Width="80" Margin="6,0,0,0"
                    Background="#3C3C3C"
                    Foreground="#CCCCCC"
                    BorderThickness="0"
                    Cursor="Hand" />
        </Grid>

        <StackPanel Grid.Row="4" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Clone" Width="80" Height="28"
                    Click="BtnClone_Click"
                    Background="#007ACC" Foreground="White"
                    BorderThickness="0" Margin="0,0,8,0"
                    Cursor="Hand" />
            <Button Content="Cancel" Width="80" Height="28"
                    Click="BtnCancel_Click"
                    Background="#3C3C3C" Foreground="#CCCCCC"
                    BorderThickness="0"
                    Cursor="Hand" />
        </StackPanel>
    </Grid>
</Window>
