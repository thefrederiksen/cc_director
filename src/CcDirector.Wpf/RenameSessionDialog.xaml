<Window x:Class="CcDirector.Wpf.RenameSessionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rename Session"
        Width="380" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#252526">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Session name:"
                   Foreground="#CCCCCC"
                   FontSize="14"
                   Margin="0,0,0,10" />

        <TextBox x:Name="NameInput" Grid.Row="1"
                 Background="#1E1E1E"
                 Foreground="#CCCCCC"
                 CaretBrush="#CCCCCC"
                 BorderBrush="#3C3C3C"
                 Padding="8,6"
                 FontSize="14"
                 KeyDown="NameInput_KeyDown" />

        <TextBlock Grid.Row="2"
                   Text="Header color:"
                   Foreground="#CCCCCC"
                   FontSize="14"
                   Margin="0,16,0,8" />

        <WrapPanel x:Name="ColorSwatchPanel" Grid.Row="3"
                   Orientation="Horizontal" />

        <StackPanel Grid.Row="4" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="OK" Width="80" Height="28"
                    Click="BtnOk_Click"
                    Background="#007ACC" Foreground="White"
                    BorderThickness="0" Margin="0,0,8,0"
                    Cursor="Hand" />
            <Button Content="Cancel" Width="80" Height="28"
                    Click="BtnCancel_Click"
                    Background="#3C3C3C" Foreground="#CCCCCC"
                    BorderThickness="0"
                    Cursor="Hand" />
        </StackPanel>
    </Grid>
</Window>
